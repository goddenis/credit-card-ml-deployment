version: "3.9"

services:
  ml-service:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: credit-default-ml-service
    ports:
      - "5000:5000"
    restart: unless-stopped